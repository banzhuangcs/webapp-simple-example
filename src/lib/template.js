/**
  js模板引擎
  提供字符串占位符、绑定数据后替换占位符
  步骤：
    1、先匹配<%%>开始的位置
    2、存储<%%>之前的字符串，将"进行转义
    3、如果<%%>里面的字符存在if else elseif ? { }这种结构语句，那么直接拼接起来
    4、如果<%%>里面是求值(表达式或变量)，那么就
    xx.push('<h1>12321</h1>');
    for() {
    xx.push(this[i])
**/
;((root, factory) => {
  if (typeof define === 'function' && define.amd) {
      
  }
})(window, () => {

});
